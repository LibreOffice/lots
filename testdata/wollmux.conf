%include "Textfragments.conf"
%include "AbsenderdatenBearbeiten.conf"
%include "PAL.conf"
%include "WhoAmI.conf"

VAR( NAME "StadtweiteVorlagen" VALUE "file:///L:/eigene_Vorlagen")
VAR( NAME "testdir" VALUE "testdata")

Textfragmente(
  # ist die Redefinition von test_f (Siehe Textfragmente.conf), diesmal mit
  # einer gültigen URL:
  (FRAG_ID "test_f" URL "${testdir}/TestFragment_a.odt")
  (FRAG_ID "WMKommandoTest" URL "${testdir}/WMKommandoTest.odt")
)